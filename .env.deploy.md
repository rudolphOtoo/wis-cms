# WIS-CMS — Clean .env for Docker Deployment
#
# Copy the contents of the code block below into your `.env` file
# on the Windows machine at C:\Users\HP\Desktop\management\.env
#
# IMPORTANT: The duplicate template section has been removed.
# Only keep ONE definition per variable — the last definition wins in dotenv.

```env
APP_NAME=WIS-CMS
APP_ENV=production
APP_KEY=base64:LA2py3o4RXLUK+hA5mTgCg5205FSzaHfsehjCptdcKs=
APP_DEBUG=true
APP_URL=http://localhost:8000

# -----------------------------------------------------------
# PROFILE SELECTION — uncomment exactly ONE pair (key + DB)
#   wis : WIS install, database wis_cms
#   mcgh: Methodist diocese, database wis_cms_mcgh
# After switching, restart the stack: docker compose up -d
# -----------------------------------------------------------
DIOCESE_PROFILE=wis
DB_DATABASE=wis_cms
# DIOCESE_PROFILE=mcgh
# DB_DATABASE=wis_cms_mcgh

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5433
DB_USERNAME=wis_admin
DB_PASSWORD=wis_secret_2024

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=database

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@wis-cms.local"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

MNOTIFY_API_KEY=DjX8rAHFkckq5fMfFgsDVJrWI
MNOTIFY_SENDER_ID=WIS

# Shared secret for the Google Form webhook
GOOGLE_FORM_WEBHOOK_SECRET=03e79f3c79b13ed8a7cd37ccaace1bd983ff821e52b6509a

# Paystack payment gateway (REQUIRED for online payments)
PAYSTACK_SECRET_KEY=sk_live_REPLACE_ME
PAYSTACK_PUBLIC_KEY=pk_live_REPLACE_ME
PAYSTACK_WEBHOOK_SECRET=REPLACE_ME
```
