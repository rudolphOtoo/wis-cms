# Capability Inventory

Registry of which profile has which flags, modules, and strategies. The
point of this file is to **prevent duplicate builds**: before adding any
new feature, check here whether another profile already has it.

Active profile: `config/diocese.php` → `config/profiles/{DIOCESE_PROFILE}.php`
(`DIOCESE_PROFILE` env, default `wis`).

Legend: ✅ enabled · ❌ disabled/absent · (key) where the value lives

---

## Capability flags (`capabilities.*`)

| Flag | `wis` | `mcgh` | Drives |
|---|---|---|---|
| `attendance.default_mode` | `register` | `headcount` | New-session default; WIS = per-member roster, MCC = Men/Women/Children door tally |
| `cells.enabled` | ✅ | ❌ | Sidebar + router show/hide `/cells/*` |
| `modules.confirmations` | ❌ | ❌ | Registers the Confirmations module (table + routes + UI). Default OFF everywhere — flip per profile to activate |

Rule: capabilities drive **UI and soft defaults only**. Authorization always
stays with Spatie permissions.

## Modules (`capabilities.modules.*`)

| Module | Path | State | Purpose |
|---|---|---|---|
| Confirmations | `app/Diocese/Modules/Confirmations/` | inert (disabled in both profiles) | Reference module proving the extensibility pattern: self-gating provider → migration + routes + React page |

## Strategies (contract → implementation)

| Contract | `wis` | `mcgh` |
|---|---|---|
| `MemberNumberGenerator` | `WisMemberNumberGenerator` → `WIS-{year}-{0001}` | `McghMemberNumberGenerator` → `MCC/{year}/{00001}` |

Bound once in `app/Diocese/Providers/DioceseServiceProvider.php` from
`Diocese::key()`.

## Strings (`strings.*`)

| Key | `wis` | `mcgh` |
|---|---|---|
| `app.title` | WIS-CMS | MCC-CMS |
| `app_name` | Wesleyan International Society | Methodist Church Ghana |
| `tagline` | With God all things are possible | With God all things are possible |
| `logo` | `/images/wis-logo.png` | `/images/mcgh-logo.png` |
| `favicon` | `/favicon.png` | `/favicon-mcgh.png` |
| `reports.footer` | Generated by WIS-CMS | Generated by MCC-CMS |

Consumed by `Diocese::string()` (PHP, incl. `Controller::pdfLogoPath()`)
and `resources/js/diocese/registry.js` `string()`/`appMeta()` (JS).
Profile strings use dotted keys stored **literally**.

## Reference data (`reference_data.*`)

| Key | `wis` | `mcgh` |
|---|---|---|
| `permissions` | 57 (full set) | same 57 |
| `roles` | 9 | same 9 (name/permissions parity) |
| `service_types` | 8 (incl. Cell Meeting, Department Meeting) | 6 (church-wide services only) |
| `finance_categories` | 25 (14 income / 11 expense) | same 25 |
| `cells` | 7 (Peace, Faithfulness, …) | none |
| `branch` | Wesleyan International Society, Kumasi | Methodist Church Ghana, Ghana |

## Boot behavior (`docker/entrypoint.sh`)

| Step | `wis` | `mcgh` |
|---|---|---|
| `app:data-migrate --import` | ✅ default input | ✅ with `--input=database/church-data-mcgh.json` (absent on fresh install → seeds reference data only) |
| `import:csv WIS_Ayeduase.csv` | ✅ | ❌ never |
| `import:csv MCC_Members.csv` | ❌ never | ✅ only when present in the image (member roster ships at repo root; idempotent upsert on every boot) |
| Other profiles | unknown slug → reference data only, warns to stderr | — |

## Tests

| Area | File |
|---|---|
| Branding / pdfLogoPath / APP_META | `tests/Feature/DioceseProfileTest.php` |
| Module gate (disabled → no table/route; enabled activates) | `tests/Feature/ConfirmationsModuleTest.php` |
| Headcount vs register (same stats shape, cross-mode 422s) | `tests/Feature/HeadcountAttendanceTest.php` |
| Member numbers per profile | `tests/Feature/MemberNumberProfileTest.php` |
| Life events (death → member status sync, birth, year report + exports, mark-deceased + edit-form sync) | `tests/Feature/LifeEventTest.php` |

---

## Life Events (deaths & births)

Year-end church review data ("those who left us", "those who were born").

| Area | Detail |
|---|---|
| Permissions | `view life events`, `manage life events` (both profiles) |
| Roles | `finance_officer` (steward) records + manages; `pastor`/`secretary` view; `super_admin` all |
| Death | free-text `first_name`/`last_name` (no member link required); optional `member_id` link; storing with a link atomically sets `member.status=deceased` + `date_of_death` (DB transaction); optional `burial_date` (must be ≥ `event_date`) |
| Death (one-click) | `POST /api/members/{id}/mark-deceased` under `edit members` — sets status + `date_of_death` and records a death life event; 422 if already deceased |
| Death (edit form sync) | saving a member with `status=deceased` requires `date_of_death` and creates a death life event if none exists for that member |
| Birth | baby `first_name`/`last_name` + `father_first_name`/`father_last_name` + `mother_first_name`/`mother_last_name`; optional `member_id` mother link; no member status change |
| Scoping | `BelongsToBranch` trait — records are branch-scoped; `member_id` must belong to the same branch |
| Report | `GET /api/reports/life-events/year?year=YYYY` (year totals + monthly breakdown) with PDF/XLSX exports under `export reports` |
| Frontend | `/life-events` page (Sidebar "Main") + `/reports/life-events/year` "Year in Review" (Sidebar Finance group) |

---

## Adding a new flag / module / strategy

1. Read this file first — the feature may already exist behind another flag.
2. **Flag**: add under `capabilities.*` in both profiles; gate Sidebar/route
   with `capability(...)` and (if scoped) the backend.
3. **Module**: copy `app/Diocese/Modules/Confirmations/` structure; register
   the provider hook is already generic (`registerModules()` in
   `DioceseServiceProvider`). Frontend page is bundled always but only
   mounted behind `CapabilityGate`.
4. **Strategy**: implement the contract, add a `match` arm in
   `DioceseServiceProvider::register()`.
5. Update this file so the next builder knows it exists.
