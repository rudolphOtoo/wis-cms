*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
*.xlsx
docker-compose.override.yml
npm-debug.log
yarn-error.log
/.fleet
/database/church-data.json
# Membership migration — derived PII artifacts (kept local, not committed)
/data/raw/
/data/cleaned/
/data/backups/
/data/reports/
# Vim swap files
*.swp
*.swo
*~
*.sql

# Deployment env docs must never carry real values (see security incident 2026-08)
.env.deploy.md
*.deploy.env

# ── Secret hygiene (hardened after the Aug 2026 leak) ──────────────
# All env-file iterations; only the annotated template is tracked.
.env*
!.env.example
# Private keys / certificates / keystores
*.pem
*.key
*.p12
*.pfx
*.jks
# Deployment secret manifests
*secret*
*credential*
!.gitleaks.toml
!config/services.example.php
